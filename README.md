# 🧩 SQL CRUD Application using Node.js and Express

This project demonstrates a **complete CRUD (Create, Read, Update, Delete)** application using **Node.js**, **Express.js**, and **MySQL** (or SQL-based database).  
It allows users to **add, view, update, and delete** user records efficiently with a clean frontend and backend integration.

---

## 🚀 Features

✅ Create new user records  
✅ Read and display data from the database  
✅ Update existing user information  
✅ Delete users by email or ID  
✅ EJS templating for dynamic frontend rendering  
✅ Organized folder structure with public assets  
✅ Environment variable support using `.env`  
✅ RESTful route structure and middleware setup  

---

## 🛠️ Technologies Used

| Layer | Technology |
|--------|-------------|
| Backend | Node.js, Express.js |
| Database | MySQL / SQL |
| Frontend | HTML, CSS, EJS |
| Environment Config | dotenv |
| Method Override | method-override |

---

## 🧰 Project Setup

### 1️⃣ Clone the Repository
```bash
git clone https://github.com/Dev26khatri/SQL_Crud.git
cd SQL_Crud;
```
2️⃣ Install Dependencies
```bash
npm install
```
3️⃣ Setup Environment Variables
```bash
Create a .env file in the root directory and add your database credentials:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=yourdatabasename
PORT=5000
```
4️⃣ Start the Server
```bash
npm start
or
node index.js
or
nodemon index.js (for Continues Server Starting without any restart)
```
